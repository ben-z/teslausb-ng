[package]
name = "teslausb"
version = "2.0.0-rs"
edition = "2021"
rust-version = "1.74"
license = "MIT"
description = "TeslaUSB dashcam snapshot and archive manager"

[dependencies]

[profile.release]
codegen-units = 1
lto = "thin"
panic = "abort"
strip = "symbols"
